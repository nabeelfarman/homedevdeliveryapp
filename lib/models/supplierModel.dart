class SupplierModel {
  final int id;
  final String title;
  final String address;
  final int category;
  final bool active;

  SupplierModel(this.id, this.title, this.address, this.category, this.active);
}
